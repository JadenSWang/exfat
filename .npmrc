# React Native / Metro cannot follow pnpm's nested symlink store reliably,
# so use a flat (npm-like) node_modules layout.
node-linker=hoisted

# The RN/Expo ecosystem has many loose peer ranges; don't fail installs on them.
strict-peer-dependencies=false
auto-install-peers=true
